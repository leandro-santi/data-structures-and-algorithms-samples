#include<iostream>
#include<stdio.h>
#include<stdlib.h>

using namespace std;

int main(){
    int a, b, c, d;
    cin >> a >> b >> c >> d;

    int p = a * c;
    int s = b + d;

    cout << p << " " << s << endl;

    return 0;
}