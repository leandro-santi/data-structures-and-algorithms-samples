#include<iostream>
#include<stdio.h>
#include<stdlib.h>
#include<math.h>

using namespace std;

int main(){
    int a, b;
    cin >> a >> b;

    float r = a / b;

    cout << pow(r, 2) << endl;

    return 0;
}